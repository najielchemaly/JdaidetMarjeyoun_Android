<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.municipality.jdeidetmarjeyoun">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:configChanges="locale|orientation"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--
             The API key for Google Maps-based APIs.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA7WN92VkSpWpGPqU6XyN5BRjTREkYnMVc" />

        <activity android:name=".loading.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"></activity>

        <activity android:name=".forgotpassword.ForgotPasswordActivity"></activity>

        <activity android:name=".event.Events_Activity"></activity>

        <activity android:name=".fees.Fees_Activity"></activity>

        <activity android:name=".signup.SignupActivity"></activity>

        <activity android:name=".aboutus.AboutusActivity"></activity>

        <activity android:name=".language.SelectLanguageActivity"></activity>

        <activity android:name=".event.EventDetailsActivity"></activity>

        <activity android:name=".news.News_Activity"></activity>

        <activity android:name=".news.NewsDetailsActivity"></activity>

        <activity android:name=".death.Deaths_Activity"></activity>

        <activity android:name=".location.LocationDetailsActivity"></activity>

        <activity android:name=".location.LocationMapActivity"></activity>

        <activity android:name=".profile.EditProfileActivity"></activity>

        <activity android:name=".login.LoginActivity"></activity>

        <activity android:name=".location.LocationActivity"
            android:windowSoftInputMode="adjustNothing"
            ></activity>

        <service
            android:name=".firebase.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <service
            android:name=".firebase.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>